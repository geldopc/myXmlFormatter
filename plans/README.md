# Implementation Plans

Generated by `/improve` on 2026-06-15.
Execute in the order below. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-editor-ui-redesign.md) | Replace split-panel with full-screen single-pane editor (no navbar, line numbers, in-place format) | P1 | M | — | DONE |
| [002](002-dracula-syntax-highlight.md) | Add Dracula-themed XML syntax highlighting to view mode | P1 | M | 001 | DONE |
| [003](003-drag-drop-file.md) | Add drag-and-drop XML file input | P2 | S | 001 | DONE |
| [004](004-shareable-url.md) | Add URL-based shareable state | P2 | M | 001 | TODO |

## Dependency notes

- **001 must be done first** — plans 002, 003, and 004 all target elements
  (`#editor-area`, `viewMode` state, `handleBackToEdit`) introduced in plan 001.
- **002, 003, 004 are independent of each other** once 001 is done and can be
  executed in any order. If executing 003 and 004 together, note that both
  modify `src/pages/Home/index.tsx` — execute them sequentially, not in parallel.

## Findings considered and rejected

- **Biome CSS parser false errors on Tailwind v4 directives** (`biome.json:20`
  missing `"tailwindDirectives": true`): trivial one-line fix — executor can
  apply directly without a plan. Add to `biome.json`:
  `"css": { "parser": { "cssModules": false, "tailwindDirectives": true } }`.
- **`forEach` returning value in `xml.ts:73`**: one-line lint fix. Change
  `children.forEach((child) => lines.push(...))` to
  `for (const child of children) { lines.push(...); }`.
- **`storageKey` default `"portfolio-theme"`** in `src/providers/Theme/index.tsx:22`:
  change string literal to `"xml-formatter-theme"`. One-line fix, no plan needed.
- **`App.tsx` uses `export default`**: change to `export function App()` and
  update `src/main.tsx:5` to `import { App } from "./App"`. Two-line fix.
- **Zero test coverage**: valuable but not blocking any of the four active plans.
  Recommend adding vitest after the UX plans land — the core surface to test is
  `src/utils/xml.ts` (`formatXml`, `minifyXml`) and `src/utils/xmlHighlight.ts`
  once plan 002 is done.
- **`esbuild` / `vite` HIGH npm advisory**: dev-only tools, not runtime code.
  Run `npm install @vitejs/plugin-react@latest` to resolve transitively;
  no plan needed.
- **`prettify()` silently dropping CDATA / PI / DOCTYPE** (`xml.ts:41–84`):
  real correctness bug but lower priority than UX changes. File a follow-up
  plan after the direction plans land.
